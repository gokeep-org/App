spring:
  application:
    name: appTest
  output:
    ansi:
      enabled: always
  http:
    encoding:
      charset: utf-8
      force: true
  profiles:
    active: dev
server:
  connection-timeout: 60000


neo4j:
  uri: bolt://localhost:7687
  username: neo4j
  password: zhongfu@2021